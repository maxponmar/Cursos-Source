/*Estructuras de Control : If Else
 * Introducción Operadores aritméticos, de comparación y lógicos */
fun main(args: Array<String>) {
    //+ - / *
    // < > == <= >=
    // ! && ||
    var a = 5
    var b = 3
    var c = -12
    //true           ||    false
    println((a != b || c < a) || (b == c))


    /*
a) a > 3                                                            b) a > b                                               c) a < c
d) c < b                                                            e) b != a                                               f) a == 8
g) b * c == 18                                                   h) a * b == -30                                    i) c / b < a
j) c / b == -11                                                    k) c / b == -4                                       l) a + c + b == 5
m) (a+b == 8) && (a-b == 2)                              n) (a+b == 8) || (a-b == 6)
o) a > 3 && b > 4 && c < 3                                 p) a > 3 && b >= 3 && c < -3
 */
}